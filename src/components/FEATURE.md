# Shared components

## TICKET-004

- **MemberCard.js** — `💬 Message` respects WhatsApp visibility (`wa_visibility` / `waVisibility`): private → disabled + lowered opacity; does not open WA when private.
- **QRCode.js** — QR encodes `https://topmlmleaders.com/u/{slug-or-id}` (fallback homepage URL if empty).
